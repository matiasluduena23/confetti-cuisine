<%- include('layout/header') %>
<section>
  <h2>Courses</h2>
  <article class="grid">
    <img src="images/dish.webp" alt="" />
    <div>
      <h3>Learn to cook cuting edge food</h3>
      <h4>Our Courses</h4>
      <% courses.forEach(course => { %>
      <p><%= course.title %></p>
      <p><%= course.description %></p>
      <p><%= course.items %></p>
      <p><%= course.zipCode %></p>
      <p><%= course._id %></p>
      <% }) %>
    </div>
  </article>
</section>

<%- include('layout/footer') %>
